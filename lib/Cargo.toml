[package]
name = "rv32-emu-lib"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4.33"

[features]
#default = ["alloc"]
alloc = []

# Enable the `alloc` feature for tests
[dev-dependencies]
rv32-emu-lib = { path = ".", features = ["alloc"] }